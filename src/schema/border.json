{
    "description": "border",
    "type": "object",
    "properties": {
        "typeId": {
            "type": "string",
            "default": "shape"
        },
        "id": {
            "type": "string",
            "minLength": 32,
            "maxLength": 36,
            "default": "$uuid"
        },
        "isEnabled": {
            "type": "boolean",
            "default": false
        },
        "fillType": {
            "$ref": "./fill-type.json"
        },
        "color": {
            "$ref": "./color.json"
        },
        "contextSettings": {
            "$ref": "./context-settings.json"
        },
        "position": {
            "$ref": "./border-position.json"
        },
        "thickness": {
            "type": "number",
            "default": 1
        },
        "gradient": {
            "$ref": "./gradient.json"
        },
        "borderStyle": {
            "$ref": "./border-style.json"
        },
        "variableRef": {
            "type": "string"
        }
    },
    "required": [
        "typeId",
        "id",
        "isEnabled",
        "fillType",
        "color",
        "position",
        "thickness",
        "borderStyle"
    ],
    "additionalProperties": false
}