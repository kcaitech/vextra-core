{
    "description": "context settings",
    "type": "object",
    "properties": {
        "typeId": {
            "type": "string"
        },
        "blenMode": {
            "$ref": "./blend-mode.json"
        },
        "opacity": {
            "type": "number",
            "default": 1
        }
    },
    "required": [
        "typeId",
        "blenMode",
        "opacity"
    ],
    "additionalProperties": false
}