{
    "description": "image shape",
    "type": "object",
    "properties": {
        "imageRef": {
            "type": "string",
            "default": "$uuid"
        },
        "patternFrame": {
            "$ref": "./pattern-frame.json"
        },
        "isClip": {
            "type": "boolean"
        }
    },
    "required": [
        "imageRef"
    ],
    "allOf": [
        {
            "$ref": "./path-shape.json"
        }
    ],
    "additionalProperties": false
}