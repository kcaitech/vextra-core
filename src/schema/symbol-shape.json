{
    "description": "symbol shape",
    "type": "object",
    "properties": {
        "isUnionSymbolShape": {
            "type": "boolean"
        },
        "unionSymbolRef": {
            "type": "string"
        },
        "overrides": {
            "type": "array",
            "items": {
                "$ref": "./override.json"
            }
        },
        "variables": {
            "type": "array",
            "items": {
                "$ref": "./variable.json"
            }
        }
    },
    "required": [
        "overrides",
        "variables"
    ],
    "allOf": [
        {
            "$ref": "./group-shape.json"
        }
    ],
    "additionalProperties": false
}