{
    "description": "text",
    "type": "object",
    "properties": {
        "typeId": {
            "type": "string",
            "default": "shape"
        },
        "paras": {
            "type": "array",
            "items": {
                "$ref": "./para.json"
            }
        },
        "attr": {
            "$ref": "./text-attr.json"
        },
        "variableRef": {
            "type": "string"
        }
    },
    "required": [
        "typeId",
        "paras"
    ],
    "additionalProperties": false
}