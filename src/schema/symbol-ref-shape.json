{
    "description": "symbol ref shape",
    "type": "object",
    "properties": {
        "refId": {
            "type": "string",
            "default": "$uuid"
        },
        "overrides": {
            "type": "array",
            "items": {
                "$ref": "./override-shape.json"
            }
        },
        "variables": {
            "type": "array",
            "items": {
                "$ref": "./variable.json"
            }
        }
    },
    "required": [
        "refId",
        "overrides",
        "variables"
    ],
    "allOf": [
        {
            "$ref": "./shape.json"
        }
    ],
    "additionalProperties": false
}